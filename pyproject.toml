[project]
name            = "bibliography"
version         = "0.1.0"
description     = "Bibtex files"
readme          = "readme.md"
requires-python = ">=3.12"
dependencies    = [
    "doot",
    "dootle",
    "bibtex-bibble",
]

[tool.uv]
prerelease="allow"

[tool.uv.sources]
